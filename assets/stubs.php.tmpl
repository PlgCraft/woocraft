<?php

/**
 * Generated by woocraft for PHPStan only. Declares the constants the
 * plugin defines at runtime via define(), so static analysis resolves
 * every file that references them. Never loaded by the plugin.
 */

define( '{{constant}}_VERSION', '0.0.0' );
define( '{{constant}}_FILE', '{{root}}/{{slug}}.php' );
define( '{{constant}}_PATH', '{{root}}/' );
define( '{{constant}}_URL', 'https://example.com/wp-content/plugins/{{slug}}/' );
define( '{{constant}}_MIN_WC_VERSION', '{{requiresWC}}' );
