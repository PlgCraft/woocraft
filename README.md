# woocraft

Build tooling for WooCommerce extensions 

Scaffolds a modern PSR-4 PHP plugin with a Vite + Tailwind v4 admin UI and
one working REST route. The lint / stan / build / package toolchain lives
**in the CLI** - scaffolded projects stay lean and get tooling upgrades
with `npm i -D woocraft@latest`.
